# Slim install: 839 MB smaller than requirements.txt, same behaviour.
#
# Versions here are the ones the reference rig actually runs, taken from its
# freeze - not hand-written. Only the three that matter are pinned; the rest
# float so a future wheel does not break the install.
#
# IMPORTANT: argostranslate is NOT in this file. Its dependency list includes
# spacy and stanza, and stanza imports torch at module level - 502 MB for
# sentence splitting that MiniSBD does instead. So the bootstrap installs it
# with --no-deps and this file carries its real requirements explicitly:
#
#   pip install -r requirements-slim.txt
#   pip install --no-deps argostranslate==1.11.0
#
# installer\bootstrap.ps1 does both. See docs/GOTCHAS.txt #26.

# --- speech to text ---
faster-whisper==1.2.1
# ctranslate2 also powers argostranslate
ctranslate2
av
tokenizers
huggingface_hub
tqdm
requests

# --- translation: argostranslate's real needs, minus spacy and stanza ---
minisbd
sentencepiece
sacremoses
packaging
joblib
regex

# --- text to speech: piper-tts needs only these two ---
piper-tts==1.8.0
onnxruntime
pathvalidate

# --- audio and input ---
sounddevice
numpy
pynput

# --- diagnostics: tools/ and tools/footprint.py ---
psutil

# --- CUDA runtime, ~2 GB. The bootstrap omits these for a CPU-only install;
# --- whisper then falls back on its own, ~1.2 s per phrase instead of ~0.2 s.
nvidia-cublas-cu12
nvidia-cudnn-cu12
